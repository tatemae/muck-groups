<%= translate('muck.groups.invite_message',
            :name => @name, 
            :message => h(@message), 
            :group => h(@group.name),
            :group_url => group_url(@group, :host=>GlobalConfig.application_url),
            :membership_url => new_group_membership_url(@group, :host=>GlobalConfig.application_url),
            :inviter_name =>h(@inviter.first_name), 
            :inviter_profile_url => profile_url(@inviter, :host=>GlobalConfig.application_url), 
            :staff_page_url => "#{GlobalConfig.website_home_url}/who-we-are/twb-staff/", 
            :emailer_name => GlobalConfig.emailed_by ) -%>